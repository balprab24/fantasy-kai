{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" && ./scripts/session-check.sh --hook",
            "timeout": 30,
            "statusMessage": "Checking what is actually true about the repo..."
          }
        ]
      }
    ]
  }
}
